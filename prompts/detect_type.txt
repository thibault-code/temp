You are classifying the visual content of a Singapore Primary 4 Mathematics worksheet image.

Examine the image and return JSON only — no explanation, no markdown, no code fences:

{
  "has_column_arithmetic": false,
  "has_place_value_objects": false,
  "has_bar_model": false,
  "has_table_or_graph": false,
  "has_geometry": false,
  "has_number_line": false,
  "has_text_only": false
}

Definitions:
- has_column_arithmetic: true if the image contains written column arithmetic (addition, subtraction, multiplication, or division laid out vertically with digits aligned in columns)
- has_place_value_objects: true if the image contains place value representations using physical objects — coins, discs, blocks, rods, or abacus diagrams arranged in columns by place value
- has_bar_model: true if the image contains a Singapore Math bar model — rectangular bars used to represent quantities, with labels or brackets showing part-whole or comparison relationships
- has_table_or_graph: true if the image contains a data table (rows and columns of values), a bar graph, a line graph, a pictograph, or a pie chart
- has_geometry: true if the image contains geometric figures — shapes, angles, or composite figures with labelled dimensions
- has_number_line: true if the image contains a number line — a horizontal line with tick marks and numerical labels representing a range of values
- has_text_only: true if the image contains questions that consist entirely of printed text and handwritten answers with no diagrams, charts, or visual representations
